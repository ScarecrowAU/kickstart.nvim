return {
  {
    'rcarriga/nvim-notify',
    config = function()
      local notify = require 'notify'
      notify.setup {
        max_width = 50,
        max_height = 10,
        timeout = 5000,
        top_down = true,
      }
    end,
  },
}
